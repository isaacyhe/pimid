# NPB (NAS Parallel Benchmarks)

5 self-contained NPB kernels (IS, CG, EP, MG, FT) with run configs in
`configs/`. Build with `make`; run all with `./run_npb.sh`.

Full documentation: [docs/benchmarks.md](../../docs/benchmarks.md#npb-nas-parallel-benchmarks)
