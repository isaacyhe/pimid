simulation:
  name: placement_06267
  mode: standalone
  duration_cycles: 10000
  workload: build/test/workloads/test_pim_workloads
memory:
  dram_type: DDR4
  channels: 2
  ranks_per_channel: 2
  chips_per_rank: 8
  bg_per_chip: 4
  banks_per_bg: 4
  subarrays_per_bank: 16
  network:
    custom_topology:
      enabled: true
      l0:
        use_default: true
      l1:
        use_default: false
        topology: CROSSBAR
        num_switches: 1
        ports_per_switch: 16
        num_endpoints: 16
      l2:
        use_default: false
        topology: TORUS_2D
        num_switches: 9
        ports_per_switch: 5
        num_endpoints: 8
      l3:
        use_default: true
      l4:
        use_default: false
        topology: FAT_TREE
        num_switches: 2
        ports_per_switch: 4
        num_endpoints: 2
      l5:
        use_default: true
pim:
  enabled: true
  num_pes: 16
  pe_placement: rank
  scheduling_policy: load_balanced
  core_model: simple
