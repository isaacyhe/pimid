simulation:
  name: comprehensive_08423
  mode: standalone
  duration_cycles: 10000
  workload: build/test/workloads/test_pim_workloads
memory:
  dram_type: LPDDR5
  channels: 8
  ranks_per_channel: 2
  chips_per_rank: 8
  bg_per_chip: 8
  banks_per_bg: 4
  subarrays_per_bank: 32
  network:
    custom_topology:
      enabled: true
      l0:
        use_default: true
      l1:
        use_default: false
        topology: H_TREE
        num_switches: 31
        ports_per_switch: 4
        num_endpoints: 32
      l2:
        use_default: true
      l3:
        use_default: false
        topology: BUS
        num_switches: 1
        ports_per_switch: 2
        num_endpoints: 2
      l4:
        use_default: true
      l5:
        use_default: false
        topology: H_TREE
        num_switches: 7
        ports_per_switch: 4
        num_endpoints: 8
pim:
  enabled: true
  num_pes: 32768
  pe_placement: bank
  scheduling_policy: load_balanced
  core_type: out-of-order
