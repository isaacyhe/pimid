simulation:
  name: single_topo_01793
  mode: standalone
  duration_cycles: 10000
  workload: build/test/workloads/test_pim_workloads
memory:
  dram_type: DDR5
  channels: 4
  ranks_per_channel: 2
  chips_per_rank: 8
  bg_per_chip: 8
  banks_per_bg: 4
  subarrays_per_bank: 32
  network:
    custom_topology:
      enabled: true
      l0:
        use_default: true
      l1:
        use_default: false
        topology: FAT_TREE
        num_switches: 32
        ports_per_switch: 4
        num_endpoints: 32
      l2:
        use_default: true
      l3:
        use_default: true
      l4:
        use_default: true
      l5:
        use_default: true
pim:
  enabled: true
  num_pes: 2048
  pe_placement: bank
  scheduling_policy: priority_based
  core_model: simple
