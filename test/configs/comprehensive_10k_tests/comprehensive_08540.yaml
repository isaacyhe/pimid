simulation:
  name: comprehensive_08540
  mode: standalone
  duration_cycles: 10000
  workload: build/test/workloads/test_pim_workloads
memory:
  dram_type: HBM3
  channels: 32
  ranks_per_channel: 2
  chips_per_rank: 4
  bg_per_chip: 8
  banks_per_bg: 4
  subarrays_per_bank: 32
  network:
    custom_topology:
      enabled: true
      l0:
        use_default: false
        topology: H_TREE
        num_switches: 3
        ports_per_switch: 4
        num_endpoints: 4
      l1:
        use_default: false
        topology: MESH_2D
        num_switches: 36
        ports_per_switch: 5
        num_endpoints: 32
      l2:
        use_default: false
        topology: FAT_TREE
        num_switches: 4
        ports_per_switch: 4
        num_endpoints: 4
      l3:
        use_default: false
        topology: TORUS_2D
        num_switches: 4
        ports_per_switch: 5
        num_endpoints: 2
      l4:
        use_default: true
      l5:
        use_default: false
        topology: CROSSBAR
        num_switches: 1
        ports_per_switch: 32
        num_endpoints: 32
pim:
  enabled: true
  num_pes: 512
  pe_placement: rank
  scheduling_policy: nearest
  core_type: in-order
