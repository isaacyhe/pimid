simulation:
  name: comprehensive_09164
  mode: standalone
  duration_cycles: 10000
  workload: build/test/workloads/test_pim_workloads
memory:
  dram_type: HBM2
  channels: 16
  ranks_per_channel: 2
  chips_per_rank: 4
  bg_per_chip: 4
  banks_per_bg: 4
  subarrays_per_bank: 16
  network:
    custom_topology:
      enabled: true
      l0:
        use_default: false
        topology: BUS
        num_switches: 1
        ports_per_switch: 4
        num_endpoints: 4
      l1:
        use_default: true
      l2:
        use_default: true
      l3:
        use_default: false
        topology: BUS
        num_switches: 1
        ports_per_switch: 2
        num_endpoints: 2
      l4:
        use_default: false
        topology: MESH_2D
        num_switches: 16
        ports_per_switch: 5
        num_endpoints: 16
      l5:
        use_default: false
        topology: MESH_2D
        num_switches: 16
        ports_per_switch: 5
        num_endpoints: 16
pim:
  enabled: true
  num_pes: 65536
  pe_placement: subarray
  scheduling_policy: data_aware
  core_type: in-order
