simulation:
  name: comprehensive_08858
  mode: standalone
  duration_cycles: 10000
  workload: build/test/workloads/test_pim_workloads
memory:
  dram_type: GDDR6
  channels: 2
  ranks_per_channel: 1
  chips_per_rank: 4
  bg_per_chip: 4
  banks_per_bg: 4
  subarrays_per_bank: 16
  network:
    custom_topology:
      enabled: true
      l0:
        use_default: true
      l1:
        use_default: true
      l2:
        use_default: true
      l3:
        use_default: false
        topology: FAT_TREE
        num_switches: 1
        ports_per_switch: 4
        num_endpoints: 1
      l4:
        use_default: true
      l5:
        use_default: true
pim:
  enabled: true
  num_pes: 2
  pe_placement: rank
  scheduling_policy: nearest
  core_model: simple
