simulation:
  name: comprehensive_09059
  mode: standalone
  duration_cycles: 10000
  workload: build/test/workloads/test_pim_workloads
memory:
  dram_type: DDR4
  channels: 1
  ranks_per_channel: 1
  chips_per_rank: 8
  bg_per_chip: 4
  banks_per_bg: 4
  subarrays_per_bank: 16
  network:
    custom_topology:
      enabled: true
      l0:
        use_default: false
        topology: H_TREE
        num_switches: 3
        ports_per_switch: 4
        num_endpoints: 4
      l1:
        use_default: false
        topology: MESH_2D
        num_switches: 16
        ports_per_switch: 5
        num_endpoints: 16
      l2:
        use_default: true
      l3:
        use_default: true
      l4:
        use_default: false
        topology: H_TREE
        num_switches: 1
        ports_per_switch: 4
        num_endpoints: 1
      l5:
        use_default: true
pim:
  enabled: true
  num_pes: 4096
  pe_placement: subarray
  scheduling_policy: nearest
  core_type: in-order
