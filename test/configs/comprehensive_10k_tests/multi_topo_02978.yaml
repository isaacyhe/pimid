simulation:
  name: multi_topo_02978
  mode: standalone
  duration_cycles: 10000
  workload: build/test/workloads/test_pim_workloads
memory:
  dram_type: HBM2
  channels: 8
  ranks_per_channel: 2
  chips_per_rank: 2
  bg_per_chip: 4
  banks_per_bg: 4
  subarrays_per_bank: 16
  network:
    custom_topology:
      enabled: true
      l0:
        use_default: false
        topology: BUS
        num_switches: 1
        ports_per_switch: 4
        num_endpoints: 4
      l1:
        use_default: true
      l2:
        use_default: true
      l3:
        use_default: true
      l4:
        use_default: true
      l5:
        use_default: false
        topology: CROSSBAR
        num_switches: 2
        ports_per_switch: 4
        num_endpoints: 8
pim:
  enabled: true
  num_pes: 16
  pe_placement: rank
  scheduling_policy: priority_based
  core_type: simple
