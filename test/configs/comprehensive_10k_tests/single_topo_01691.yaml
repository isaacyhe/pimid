simulation:
  name: single_topo_01691
  mode: standalone
  duration_cycles: 10000
  workload: build/test/workloads/test_pim_workloads
memory:
  dram_type: HBM2
  channels: 16
  ranks_per_channel: 2
  chips_per_rank: 4
  bg_per_chip: 4
  banks_per_bg: 4
  subarrays_per_bank: 16
  network:
    custom_topology:
      enabled: true
      l0:
        use_default: true
      l1:
        use_default: false
        topology: CROSSBAR
        num_switches: 4
        ports_per_switch: 4
        num_endpoints: 16
      l2:
        use_default: true
      l3:
        use_default: true
      l4:
        use_default: true
      l5:
        use_default: true
pim:
  enabled: true
  num_pes: 2048
  pe_placement: bank
  scheduling_policy: load_balanced
  core_type: simple
