simulation:
  name: single_topo_02381
  mode: standalone
  duration_cycles: 10000
  workload: build/test/workloads/test_pim_workloads
memory:
  dram_type: DDR5
  channels: 2
  ranks_per_channel: 1
  chips_per_rank: 8
  bg_per_chip: 8
  banks_per_bg: 4
  subarrays_per_bank: 16
  network:
    custom_topology:
      enabled: true
      l0:
        use_default: true
      l1:
        use_default: true
      l2:
        use_default: true
      l3:
        use_default: true
      l4:
        use_default: false
        topology: MESH_2D
        num_switches: 4
        ports_per_switch: 5
        num_endpoints: 2
      l5:
        use_default: true
pim:
  enabled: true
  num_pes: 8192
  pe_placement: subarray
  scheduling_policy: nearest
  core_type: simple
