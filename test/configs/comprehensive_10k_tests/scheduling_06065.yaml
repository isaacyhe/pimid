simulation:
  name: scheduling_06065
  mode: standalone
  duration_cycles: 10000
  workload: build/test/workloads/test_pim_workloads
memory:
  dram_type: DDR5
  channels: 2
  ranks_per_channel: 1
  chips_per_rank: 8
  bg_per_chip: 8
  banks_per_bg: 4
  subarrays_per_bank: 16
pim:
  enabled: true
  num_pes: 16
  pe_placement: chip
  scheduling_policy: nearest
  core_model: simple
