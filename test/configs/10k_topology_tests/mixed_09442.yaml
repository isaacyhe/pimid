simulation:
  name: mixed_09442
  mode: standalone
  duration_cycles: 10000
memory:
  dram_type: GDDR6
  channels: 2
  ranks_per_channel: 1
  chips_per_rank: 4
  bg_per_chip: 4
  banks_per_bg: 4
  subarrays_per_bank: 16
  network:
    custom_topology:
      enabled: true
      l0:
        use_default: false
        topology: MESH_2D
        num_switches: 4
        ports_per_switch: 5
        num_endpoints: 4
      l1:
        use_default: false
        topology: MESH_2D
        num_switches: 16
        ports_per_switch: 5
        num_endpoints: 16
      l2:
        use_default: true
      l3:
        use_default: true
      l4:
        use_default: false
        topology: TORUS_2D
        num_switches: 4
        ports_per_switch: 5
        num_endpoints: 2
      l5:
        use_default: true
external_models:
  network:
    enabled: false
  memory:
    enabled: false
pim:
  enabled: true
  num_pes: 128
  pe_placement: bank
  scheduling_policy: nearest
