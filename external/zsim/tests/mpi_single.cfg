// Single MPI rank for PIN testing
sys = {
    cores = {
        simple = {
            type = "Simple";
            cores = 1;
            icache = "l1i";
            dcache = "l1d";
        };
    };

    lineSize = 64;

    caches = {
        l1d = { caches = 1; size = 32768; };
        l1i = { caches = 1; size = 32768; };
        l2 = { caches = 1; size = 1048576; children = "l1d|l1i"; };
    };
};

sim = {
    phaseLength = 10000;
    maxTotalInstrs = 500000000;
    schedQuantum = 100;
    aslr = false;
};

process0 = { command = "/tmp/real_mpi_compute_ompi"; };
